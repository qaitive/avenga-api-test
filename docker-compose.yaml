version: '3.8'

services:
  test-runner:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/usr/src/app
      - ./mochawesome-report:/usr/src/app/mochawesome-report
    command: npm test